/**
 * @beacon/github — a small, dependency-free GitHub REST client that collects a
 * {@link RepositorySnapshot} for a repository, plus the `owner/repo` parser and
 * the {@link GitHubError} type shared across Beacon.
 */
export * from './client';

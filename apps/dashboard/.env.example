# Point the analytics dashboard at a running Beacon API to use live analysis
# and real stored history. When unset, the app renders from @beacon/core demo
# fixtures with synthesized history (demo mode).
NEXT_PUBLIC_API_URL=

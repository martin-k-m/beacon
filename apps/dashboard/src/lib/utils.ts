/**
 * Presentational helpers live in the shared @beacon/ui package. This module
 * re-exports them so local `@/lib/utils` imports stay short and consistent with
 * the rest of the app.
 */
export * from '@beacon/ui/utils';
